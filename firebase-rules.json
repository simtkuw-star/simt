{
  "rules": {
    "teachers": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": false
      }
    },
    "students": {
      ".read": "auth != null && root.child('teachers').child(auth.uid).val() == true",
      "$uid": {
        ".read": "auth != null && (auth.uid == $uid || root.child('teachers').child(auth.uid).val() == true)",
        "profile": {
          ".write": "auth != null && auth.uid == $uid"
        },
        "work": {
          "draft": {
            ".write": "auth != null && auth.uid == $uid"
          },
          "submittedAt": {
            ".write": "auth != null && auth.uid == $uid"
          },
          "training": {
            ".write": "auth != null && auth.uid == $uid"
          },
          "rubric": {
            ".write": "auth != null && root.child('teachers').child(auth.uid).val() == true"
          },
          "rubricTotal": {
            ".write": "auth != null && root.child('teachers').child(auth.uid).val() == true && newData.isNumber()"
          },
          "rubricLevel": {
            ".write": "auth != null && root.child('teachers').child(auth.uid).val() == true"
          },
          "teacherComment": {
            ".write": "auth != null && root.child('teachers').child(auth.uid).val() == true"
          },
          "evaluatedAt": {
            ".write": "auth != null && root.child('teachers').child(auth.uid).val() == true"
          },
          "evaluatedBy": {
            ".write": "auth != null && root.child('teachers').child(auth.uid).val() == true"
          },
          "updatedAt": {
            ".write": "auth != null && (auth.uid == $uid || root.child('teachers').child(auth.uid).val() == true)"
          }
        }
      }
    },
    "siteStats": {
      ".read": false,
      "totalLogins": {
        ".write": "auth != null && ((data.exists() && newData.val() == data.val() + 1) || (!data.exists() && newData.val() == 1))"
      },
      "lastLoginAt": {
        ".write": "auth != null"
      },
      "lastStudents": {
        "$uid": {
          ".write": "auth != null && auth.uid == $uid"
        }
      }
    }
  }
}
